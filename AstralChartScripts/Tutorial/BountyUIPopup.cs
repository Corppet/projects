using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BountyUIPopup : PopupObject
{
    /*
    public void ButtonPressed()
    {
        if (!buttonPressed && isOpened)
        {
            StartCoroutine(BufferNextPopup(triggerDelay));
            buttonPressed = true;
        }
    }

    public override void Update(){
        if (!buttonPressed && isOpened)
        {
            if(triggerKey != null && Input.GetKeyDown(triggerKey)){
                StartCoroutine(BufferNextPopup(triggerDelay));
                buttonPressed = true;
            }
        }
    }*/
}