using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CrewUIPopup : PopupObject
{
/*
    public override void Update(){
        if(!buttonPressed && isOpened && Input.GetKeyDown(triggerKey)){
            StartCoroutine(BufferNextPopup(triggerDelay));
            buttonPressed = true;
        }
    }

    public void ButtonPressed()
    {
        if (!buttonPressed && isOpened)
        {
            StartCoroutine(BufferNextPopup(triggerDelay));
            buttonPressed = true;
        }
    }*/
}