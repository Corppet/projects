using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MissionPopup : PopupObject
{
    /*
    [SerializeField] private BountyButton bb;
    //[SerializeField] private IntroductionPopup

    public override void Update(){
        if(!buttonPressed && bb.getStatus() == BountyStatus.InPursuit){
            StartCoroutine(BufferNextPopup(triggerDelay));
            buttonPressed = true;
        }
    }

    public void ButtonPressed()
    {
        if (!buttonPressed && isOpened)
        {
            StartCoroutine(BufferNextPopup(triggerDelay));
            buttonPressed = true;
        }
    }*/
}
