using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IntroductionPopup : PopupObject
{
    /*
    public override void Update()
    {
        if (isOpened && !buttonPressed)
        {
            StartCoroutine(BufferNextPopup(triggerDelay));
            buttonPressed = true;
        }
    }*/
}