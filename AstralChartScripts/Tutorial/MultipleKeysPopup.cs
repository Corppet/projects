using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MultipleKeysPopup : PopupObject
{
    /*
    [SerializeField] private List<KeyCode> triggerKeys;

    public override void Update(){
        foreach(KeyCode k in triggerKeys){
            if (Input.GetKeyDown(k) && !buttonPressed)
            {
                StartCoroutine(BufferNextPopup(triggerDelay));
                buttonPressed = true;
            }
        }
    }*/
}
